<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ahshing.qiupprogrammesenquiryapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".FilterProgrammes"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:label="Programmes Enquiry" />

        <activity
            android:name=".CaptureData"
            android:label="Key-in Particulars" />

        <activity
            android:name=".ResultsOfFiltering"
            android:label="Eligibility Result" />

        <activity
            android:name=".InterestedProgrammeAndEnglishProficiency"
            android:label="Programmes Enquiry" />

        <activity
            android:name=".InterestProgramme"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:label="Programmes Enquiry">
        </activity>
    </application>

</manifest>